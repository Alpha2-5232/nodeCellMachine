const totalPrimes = 100
let allPrimes = []
let primesFound = 0;
while(primesFound < totalPrimes) {
	
}

function isPrime(n) {
	let i = 0
	while((allPrimes[i] < Math.ceil(sqrt(n)) )&& (i < allPrimes.length)) {

	}
	return;
}